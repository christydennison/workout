<h1>Listing memberships</h1>

<table>
  <tr>
    <th>Athena</th>
  </tr>

<% @memberships.find_by_user_id(:user_id) do |membership| %>
  <tr>
    <td><%= link_to 'Edit', edit_membership_path(membership) %></td>
    <td><%= link_to 'Destroy', membership, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />