<% if flash[:alert] %>
<p id="notice"><%= flash[:alert] %></p>
<% end %>

<h1>Workout with me</h1>
<h2>A great place to find people to workout with!</h2>
<% logger.debug "\n \n ********* \n Debugger works in view! ********* \n \n" %>

<%= render 'events/index', :events => Event.all %>

<div> <%= link_to 'New Event', new_event_url %></div>
<% unless current_user %>
  <div> <%= link_to 'Login', login_url %></div>
  <div> <%= link_to 'Register', register_url %></div>
<% end %>
<% if current_user %>
  <div> <%= link_to 'Logout', logout_url %></div>
<% end %>
<% debug "CURRENT USER: #{current_user}" %>

<br />
